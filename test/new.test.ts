describe("test", () => {
    jest.setTimeout(30000);

    beforeAll(async done => {
        done();
    });

    afterAll(async () => {
    })

    it("test", async () => {
        expect(1).toBe(1);
    });

})
